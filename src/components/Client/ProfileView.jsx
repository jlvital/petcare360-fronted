import React from 'react';
import { Typography } from '@mui/material';

const ProfileView = () => (
  <Typography variant="body1">Aquí podrás editar tu perfil personal.</Typography>
);

export default ProfileView;
