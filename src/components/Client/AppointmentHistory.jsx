import React from 'react';
import { Typography } from '@mui/material';

const AppointmentHistory = () => (
  <Typography variant="body1">Aquí podrás ver tus citas pasadas y futuras.</Typography>
);

export default AppointmentHistory;
